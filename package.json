{
  "name": "vneclassno-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "VneClassno monorepo: backend, pwa, telegram mini app",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "dev": "npm run --workspaces dev",
    "lint": "npm run --workspaces lint",
    "typecheck": "npm run --workspaces typecheck",
    "test": "npm run --workspaces test"
  },
  "engines": {
    "node": ">=20"
  }
}
